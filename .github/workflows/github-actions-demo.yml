name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  tests-unitaires:
    runs-on: goland
    steps:
      - uses: actions/checkout@v2
      - run: cd clean
      - run: go test -v
      - run:  cd ..\pokedex\
      - run: go test -v
      - run:  cd ..\presenter\
      - run: go test -v